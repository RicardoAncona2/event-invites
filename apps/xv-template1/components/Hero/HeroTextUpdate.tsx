'use client'

import { Typography } from "@mui/material"
import EditButton from "../utils/EditButton"
import { updateHero } from "apps/xv-template1/lib/db"

const HeroTextUpdate = () => <Typography
    variant='h1'
    color='primary'
    gutterBottom
    sx={{
        fontFamily: 'var(--font-playfair-display)',
    }}
>

</Typography>
export default HeroTextUpdate